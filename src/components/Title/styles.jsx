import styled from "styled-components";

export const TitleContainer = styled.h1 `
    color: var(--Black);
    font-size: 3rem;
    &.Black {
        color: var(--Black);
    }
    &.Yellow {
        color: var(--Primary);
    }
` 